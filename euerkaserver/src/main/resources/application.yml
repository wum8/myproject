server:
  port: 10086
spring:
  application:
    name: eureka-server #服务名称，或作为服务的唯一标识
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    register-with-eureka: false #不注册自己
    fetch-registry: false #自己不拉取服务
